# Reviewer: BOUNCE — qa (Quinn) — 108-A-05

## Verdict

**BOUNCE**. Three independent BLOCKERs land on Story 108-A-05 despite the
worker's self-report of "all 2019 CLI tests + 9874 core tests pass". The
test suite passes because the failing surfaces have no test coverage —
they are user-facing artifacts (static bundled completions) and dogfood
infrastructure (probe bash bodies) that fall outside the per-package
vitest gate. Plus the CHANGELOG DoD item is unticked.

Bounce reason summary:

1. BLOCKER — `apps/cli/scripts/generate-completions.ts` still builds a
   **flat** Commander tree at build time; the bundled `completions/assignee.{zsh,bash,fish}`
   artifacts ship listing flat `plan apply destroy ...` as the top-level
   completions, NOT `infra admin dev`. Direct contradiction of AC #5 and
   Axis G/H. (Runtime `dev completions <shell>` is correct — only the
   bundled static files are wrong.)
2. BLOCKER — `apps/cli/scripts/PROBE_MANIFEST.yaml` probe BODIES contain
   38 legacy `$PROBE_CLI_BIN plan ...` invocations; the runner
   (`pre-close-probes.sh` line 581) executes the bash body, not the
   `cmd:` schema line. Every probe these touch will fail with
   `error: unknown command 'plan'` when `pnpm pre-close-probes` runs.
3. BLOCKER — CHANGELOG entry for 108-A-05 missing from `CHANGELOG.md` and
   from `docs/engineering/changelog-history.md` — DoD item L155
   explicit: "CHANGELOG entry for 108-A-05 added in closing commit".

Plus one HIGH (drift between completion-generator unit test and the
bundled artifact — the test passes because it builds its own noun-grouped
tree inline; the production build script does NOT).

---

## Findings

| #   | Severity | Category                | File:line                                                                                                      | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Proposed fix                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Effort |
| --- | -------- | ----------------------- | -------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ |
| 1   | BLOCKER  | CORRECTNESS / DRIFT     | `apps/cli/scripts/generate-completions.ts:48-78`                                                               | `program.addCommand(planCommand); program.addCommand(applyCommand); program.addCommand(initCommand); ...` — 17 flat `program.addCommand(...)` calls under `buildProgram()`. Confirmed shipping artifact: `head -30 apps/cli/completions/assignee.bash` → `commands="plan apply init completions describe destroy drift optimize list setup status reconcile doctor restore-provisions audit-verify update discover version"`. Same flat list in `assignee.zsh` (`'plan:...'`, `'apply:...'`, `'status:...'`) and `assignee.fish` (`complete -c assignee -n __fish_use_subcommand -a plan`).                                                                                                                                                 | Refactor `buildProgram()` in `generate-completions.ts` to mirror `apps/cli/src/index.ts`: create `infraGroup`, `adminGroup`, `devGroup` via `new Command(CommandGroup.X).description(CommandDescription.X)`, register leaves under their groups, then `program.addCommand(infraGroup)` etc. Re-run `pnpm --filter assignee build` to regenerate the three artifact files.                                                                                                                                                                                                | M      |
| 2   | BLOCKER  | CORRECTNESS / PROBE-GAP | `apps/cli/scripts/PROBE_MANIFEST.yaml` (38 lines, samples below)                                               | `grep -cE '\$PROBE_CLI_BIN (plan\|apply\|destroy\|drift\|reconcile\|optimize\|restore-provisions\|status\|list\|doctor\|describe\|audit-verify\|init\|setup\|update\|completions\|discover\|version)\b' apps/cli/scripts/PROBE_MANIFEST.yaml` → `38`. Confirmed grep also returns `0` for `\$PROBE_CLI_BIN (infra\|admin\|dev)\b`. Example lines: `:75: $PROBE_CLI_BIN plan \\`, `:106: $PROBE_CLI_BIN apply "create S3 bucket named invalid..name"`, `:298: cli_out="$($PROBE_CLI_BIN plan --help 2>&1)"`, `:422: check_cmd "apply" $PROBE_CLI_BIN apply`, `:424: check_cmd "plan" $PROBE_CLI_BIN plan`. Runner `pre-close-probes.sh:581` confirms `bash -c "...; $(cat \"$probe_file\")"` executes the BODY (not the `cmd:` declaration). | Sed-replace every `$PROBE_CLI_BIN <leaf>` invocation inside `probe: \|` bodies to `$PROBE_CLI_BIN <group> <leaf>`. Map: plan/apply/destroy/drift/reconcile/optimize/restore-provisions → `infra <leaf>`; status/list/doctor/describe/audit-verify → `admin <leaf>`; init/setup/update/completions/discover/version → `dev <leaf>`. Also fix line 422/424 `check_cmd "apply"` / `check_cmd "plan"` labels to `"infra apply"` / `"infra plan"`. Run `bash apps/cli/scripts/pre-close-probes.sh` against a representative probe (e.g. e96.W1.B1) to verify post-fix exit 0. | M      |
| 3   | BLOCKER  | CHANGELOG               | `CHANGELOG.md` (no entry) + `docs/engineering/changelog-history.md` (no entry)                                 | `grep -n "108-A-05\|verb-group\|noun-group" CHANGELOG.md` → empty. Same for `changelog-history.md`. DoD line 155: `[ ] CHANGELOG entry for 108-A-05 added in closing commit`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Add `## [Unreleased] → ### Changed → cli(api-surface): restructure CLI into noun-grouped command tree (Story 108-A-05)` block to `CHANGELOG.md` under `[Unreleased]`. Cover: 3 noun groups (infra/admin/dev), 18 leaves, depth-2 `configureHelp` cascade, snapshot test, 14 e2e files migrated, BREAKING for any scripted users (none ship yet — pre-publish).                                                                                                                                                                                                           | S      |
| 4   | HIGH     | DRIFT                   | `apps/cli/src/services/completion-generator.test.ts:25-95` vs `apps/cli/scripts/generate-completions.ts:48-78` | Unit test under `completion-generator.test.ts` builds its OWN noun-grouped Command tree (`new Command("infra")` etc. lines 28-60) and asserts the generator emits noun-grouped output. Production build script builds a FLAT tree (Finding #1). Unit test therefore passes while the shipping artifact is wrong — no regression test catches drift between `generate-completions.ts` and `index.ts`.                                                                                                                                                                                                                                                                                                                                        | After fixing Finding #1, add a regression test (e.g. `generate-completions-artifact-parity.test.ts`) that imports `buildProgram()` from `generate-completions.ts` (export it if needed) and asserts the returned root has exactly 3 sub-commands named `infra`, `admin`, `dev`. Alternative: refactor `generate-completions.ts` to import a shared `buildAssigneeProgram()` factory from `apps/cli/src/index.ts` so there is ONE source of truth for the tree shape.                                                                                                     | M      |
| 5   | HIGH     | PROBE-GAP               | `apps/cli/src/__tests__/commander-tree-snapshot.test.ts:38-186`                                                | Snapshot test constructs its own Command tree in test code (lines 38-101) and snapshots THAT, instead of asserting on the production `apps/cli/src/index.ts` program object. If a future change forgets to register a leaf in `index.ts` but the test still imports the leaf module, the snapshot stays green while the production binary is broken. Mirrors the Finding #4 anti-pattern.                                                                                                                                                                                                                                                                                                                                                   | Refactor `index.ts` to export a `buildAssigneeProgram(): Command` factory (or move the program-building lines into a separate module). Update the snapshot test to call that factory directly so the snapshot reflects the SAME tree that ships in `dist/index.js`. The runtime `dev completions` command can also use this factory instead of `while (root.parent !== null) root = root.parent`.                                                                                                                                                                        | M      |
| 6   | MED      | DRIFT                   | `apps/cli/scripts/generate-completions.ts:55-61`                                                               | Comment block says "Story 50-3 trimmed the list from 18 → 13 by removing clean, cache, patterns, types, whoami. Story 108-A-03 round-2 fix: added restore-provisions, audit-verify, update, and discover (all four were previously missing from this generator…)". This comment is no longer accurate after 108-A-05 — and the file itself remains uncorrected. The stale comment is documentary evidence that this file was never reviewed during 108-A-05.                                                                                                                                                                                                                                                                                | Replace the comment block to reference Story 108-A-05 noun-group structure once Finding #1 is fixed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | S      |
| 7   | MED      | PROBE-GAP               | `apps/cli/src/__tests__/commander-tree-snapshot.test.ts` (entire file)                                         | No probe for Axis B (typo handling). I manually verified `./dist/index.js infra pla` returns `error: unknown command 'pla' (Did you mean plan?)` exit 1 — works. But there is no automated test asserting this behaviour; Axis B is not in the snapshot file.                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Add an `it("typoed subcommand under noun group emits Commander did-you-mean", ...)` that spawns the CLI binary or stubs Commander and asserts the error message + exit code. Tied to Axis B in the story spec.                                                                                                                                                                                                                                                                                                                                                           | S      |
| 8   | LOW      | DRIFT                   | runtime metric `doc-lint: ... commands=3 ...`                                                                  | doc-lint counts `program.addCommand(...)` and now reports `commands=3` because only the three noun groups are direct children of `program`. No doc strings hardcode `commands=18` so doc-lint still passes — but the metric label is misleading. Future docs that refer to "all 18 CLI commands" via the doc-lint counter will silently regress.                                                                                                                                                                                                                                                                                                                                                                                            | Update `apps/cli/scripts/doc-lint.mjs:99-115` `countCommands()` to walk one level deeper when a child has its own `program.addCommand(...)` block — or document that `commands=N` reports noun-group count, not leaf count.                                                                                                                                                                                                                                                                                                                                              | S      |

---

## Probe coverage matrix (Axes A–L from story spec)

| Axis                                                    | Status | Evidence                                                                                                                                                                                                                                             |
| ------------------------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A — 18 noun-group paths each accept `--help` exit 0     | ⚠️     | `commander-tree-snapshot.test.ts:38-289` snapshots the tree, but NO test invokes `--help` against each of the 18 paths. Manual probe: `./dist/index.js infra plan --help` → exit 0 ✓. 18 invocations not exercised automatically.                    |
| B — Typo handling `infra pla` returns help/did-you-mean | ❌     | Manually verified working (`error: unknown command 'pla' (Did you mean plan?)`), but no automated assertion. Snapshot test does not cover this.                                                                                                      |
| C — `--help` at each depth                              | ⚠️     | Manually verified: root, group, leaf all render. Snapshot test asserts tree shape but not `--help` rendering at each level.                                                                                                                          |
| D — `--verbose` at sub-command                          | ✅     | `./dist/index.js infra plan --help` lists `--verbose` global flag. Depth-2 `configureHelp` loop at `apps/cli/src/index.ts:219-224`.                                                                                                                  |
| E — `--json` at sub-command                             | ✅     | Same — `--json` listed under `infra plan --help`.                                                                                                                                                                                                    |
| F — `--output` at sub-command                           | ✅     | Same — `-o, --output <format>` listed.                                                                                                                                                                                                               |
| G — zsh completion enumerates `infra/admin/dev`         | ❌     | **BLOCKER #1**: `apps/cli/completions/assignee.zsh` lists `'plan:...'`, `'apply:...'`, NOT `'infra:...'`. Runtime `dev completions zsh` IS correct — but bundled static file is stale.                                                               |
| H — Bash + fish completions run for all three shells    | ❌     | **BLOCKER #1**: All three bundled files (`.zsh`/`.bash`/`.fish`) are flat. `assignee.bash` line 11: `commands="plan apply init completions ..."`. `assignee.fish`: `complete ... -a plan`, `-a apply`, etc.                                          |
| I — Commander tree snapshot, non-empty, 18 cmds         | ✅     | `commander-tree-snapshot.test.ts:110-185` inline-snapshots a 3-group / 18-leaf tree. Passes — but see Finding #5 (snapshot tests local tree, not production tree).                                                                                   |
| J — Legacy flat path rejection                          | ✅     | `./dist/index.js plan` → `error: unknown command 'plan'` exit 1 ✓.                                                                                                                                                                                   |
| K — `--cost-detail` survives on `infra plan`            | ✅     | `shipped-wired-contract.test.ts:244` retains `--cost-detail` in `PROBE_WHITELIST_FLAGS`. `./dist/index.js infra plan --cost-detail "x" --json` would route to plan action (not manually exercised; option registered on `planCommand` is unchanged). |
| L — `dev discover` launches interactive picker          | ⚠️     | `index.ts:207` registers `discoverCommand` under `devGroup`. `apps/cli/src/commands/discover/discover.test.ts` updated to `runCli(["dev", "discover", ...])` (per diff). Interactive picker not manually exercised but unit tests pass.              |

**5 ⚠️ / 2 ❌ / 5 ✅**. Two outright fails (Axis G, Axis H) both originate from BLOCKER #1.

---

## Gate state

| Gate                    | Status             | Evidence                                                                                                                                                                                                                                                                                                         |
| ----------------------- | ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| build                   | ✅ PASS            | `pnpm build` → `Tasks: 4 successful, 4 total, Cached: 4 cached, Time: 81ms >>> FULL TURBO`.                                                                                                                                                                                                                      |
| lint                    | ✅ PASS            | `pnpm lint` → `4 successful, 4 cached, FULL TURBO`.                                                                                                                                                                                                                                                              |
| check-types             | ✅ PASS            | `pnpm check-types` → `6 successful, 6 cached`.                                                                                                                                                                                                                                                                   |
| citation-lint           | ✅ PASS            | `citation-lint: scanned 101 files, 351 citations, 0 broken`.                                                                                                                                                                                                                                                     |
| doc-lint                | ⚠️ PASS-WITH-DRIFT | `doc-lint: patterns=13 types=38 strategies=38 decomposers=38 commands=3 graphNodes=15` — runs to completion but `commands=3` is the noun-group count, not the 18-leaf count (Finding #8).                                                                                                                        |
| CLI test count          | ✅ MATCH           | `pnpm --filter assignee test` → `Test Files 122 passed \| 32 skipped (154); Tests 2019 passed \| 148 skipped (2167)`. Matches worker claim of 2019. (32 skipped files are e2e under RUN_E2E gate.)                                                                                                               |
| Core test count         | ✅ MATCH           | `pnpm --filter @assignee/core test` → `Test Files 390 passed (390); Tests 9874 passed (9874)`. Matches worker claim of 9874.                                                                                                                                                                                     |
| `pnpm -r test:coverage` | NOT-RUN            | Coordinator gate; not run by reviewer per discipline. Worker did not produce a coverage transcript so the DoD line "Coordinator's serial `pnpm -r test:coverage` passes" is UNVERIFIED.                                                                                                                          |
| Probes runtime          | ❌ FAIL            | `bash apps/cli/scripts/pre-close-probes.sh --strict-multi-variation` passes (schema-only). Full `bash apps/cli/scripts/pre-close-probes.sh` not executed by reviewer; based on 38 legacy invocations in bodies + Commander rejection of flat paths (`exit 1` on `assignee plan`), every touched probe will fail. |

---

## Self-claim audit (worker's 9 claims)

| #   | Worker claim                                                                                | Verdict                   | Evidence                                                                                                                                                                                                                                                                         |
| --- | ------------------------------------------------------------------------------------------- | ------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | 18 commands reorganized into 3 noun groups (infra 7 / admin 5 / dev 6)                      | **CONFIRMED**             | `apps/cli/src/index.ts:187-208` registers 7+5+6 leaves under the three groups. Snapshot test asserts the counts.                                                                                                                                                                 |
| 2   | Depth-2 `configureHelp` loop done in `apps/cli/src/index.ts`                                | **CONFIRMED**             | `apps/cli/src/index.ts:219-224` — nested for-loops over `program.commands` and each `group.commands`. Manual `infra plan --help` shows `--verbose`/`--output`/`--json`.                                                                                                          |
| 3   | `CommandGroup` + `CommandPath` constants in core                                            | **CONFIRMED**             | `packages/core/src/constants/commands.ts:15-83` defines `CommandGroup` (3 entries), `CommandName` (18 leaves), `CommandPath` (18 full paths), `CommandDescription` (group + leaf descs).                                                                                         |
| 4   | Completion generator rewritten (recursive walker, 2-level shell completions)                | **DISPUTED**              | The per-shell generators (`bash.ts`, `zsh.ts`, `fish.ts`) and `extract.ts` ARE updated for recursion. BUT `apps/cli/scripts/generate-completions.ts` still builds a flat tree at build time → the bundled `assignee.{zsh,bash,fish}` shipped artifacts are FLAT. See BLOCKER #1. |
| 5   | Commander tree snapshot test created                                                        | **CONFIRMED-WITH-CAVEAT** | `apps/cli/src/__tests__/commander-tree-snapshot.test.ts` exists (290 lines, 5 tests). Caveat: snapshots a locally-built tree, not the production program (Finding #5).                                                                                                           |
| 6   | Variant-matrix `CLI_COMMANDS` updated to 18 noun-grouped paths; drift-guard preserved at 18 | **CONFIRMED**             | `packages/core/src/__tests__/variant-matrix/index.ts:127-149` lists 18 entries with `"infra plan"` form. `drift-guard.test.ts:195` asserts `expect(CLI_COMMANDS.length).toBe(18)`.                                                                                               |
| 7   | `PROBE_MANIFEST.yaml`: 115 probe commands updated                                           | **DISPUTED**              | The `cmd:` schema declarations are noun-grouped (sample lines 64-218 etc.). The probe `probe: \|` BODIES that actually execute are NOT: 38 `$PROBE_CLI_BIN <leaf>` invocations remain. See BLOCKER #2.                                                                           |
| 8   | All 14 E2E test files updated                                                               | **CONFIRMED**             | `grep -rn 'runCli(\["plan"\|"apply"\|"destroy"\|…' apps/cli/src/ --include="*.ts" \| wc -l` → `0`. All `runCli(...)` invocations in E2E files are noun-grouped per the diff.                                                                                                     |
| 9   | All 2019 CLI tests + 9874 core tests pass                                                   | **CONFIRMED**             | Re-ran both: 2019/2019 CLI pass, 9874/9874 core pass. (Note: tests pass because the broken surfaces — static completions and probe bodies — are not exercised by vitest.)                                                                                                        |

**Score: 6/9 CONFIRMED, 2/9 DISPUTED, 1/9 CONFIRMED-WITH-CAVEAT.** Below the 7/9 BOUNCE threshold from the briefing.

---

## Must-fix items for the dev worker

Address each in order; commit one wave; re-request review.

1. **Fix `generate-completions.ts` to build a noun-grouped tree** (BLOCKER #1).
   - Mirror `apps/cli/src/index.ts:174-212`: create `infraGroup`, `adminGroup`, `devGroup` from `CommandGroup` + `CommandDescription` (import from `@assignee/core`), nest the 18 leaves, then `program.addCommand(infraGroup/adminGroup/devGroup)`.
   - Run `pnpm --filter assignee build` and verify all three `apps/cli/completions/assignee.{zsh,bash,fish}` artifacts now contain `infra admin dev` as top-level completions and the corresponding `case` blocks for sub-commands.
   - Add a regression test (e.g. `apps/cli/src/services/completion-generator/artifact-parity.test.ts`) that runs `buildProgram()` from the script (export it) and asserts root commands are exactly `["infra", "admin", "dev"]`.

2. **Migrate all probe BODIES in `PROBE_MANIFEST.yaml`** (BLOCKER #2).
   - sed-replace every `$PROBE_CLI_BIN <leaf>` to `$PROBE_CLI_BIN <group> <leaf>` inside `probe: \|` blocks. 38 lines to touch; see findings table for the group→leaf mapping.
   - Fix `check_cmd "<leaf>" $PROBE_CLI_BIN <leaf>` labels on lines 422 + 424 (`"infra apply"`, `"infra plan"`).
   - Locally execute one representative probe (e.g. `bash apps/cli/scripts/pre-close-probes.sh --scope 'e96.W1.B1'`) to verify post-fix exit 0.

3. **Add `CHANGELOG.md` entry** (BLOCKER #3).
   - Under `## [Unreleased]`, add a new `### Changed` block summarising: 3 noun groups, 18 leaves, breaking change rationale (pre-publish), AC #1–#10 list, link to story spec `_bmad-output/implementation-artifacts/epic-108-A-05-verb-grouped-command-tree.md`.
   - Optionally mirror into `docs/engineering/changelog-history.md` if that file requires per-story entries.

4. **Refactor to a single program-builder factory** (HIGH #4 + #5).
   - Export a `buildAssigneeProgram(): Command` factory from a new module (e.g. `apps/cli/src/program.ts`). Have `apps/cli/src/index.ts`, `apps/cli/scripts/generate-completions.ts`, `apps/cli/src/__tests__/commander-tree-snapshot.test.ts`, and `apps/cli/src/commands/completions.ts` all consume that factory. This eliminates the three-way drift (production index, build-time generator, snapshot test) and is the right architectural shape for the v1.0 API freeze.

5. **Add automated probe for Axis B** (MED #7).
   - Add a unit test that spawns the CLI with `infra pla` and asserts stderr contains `error: unknown command 'pla'` and `Did you mean plan?`, exit 1.

6. **Update doc-lint commands counter or its label** (LOW #8).
   - Decide whether `commands=3` (noun-group count) or `commands=18` (leaf count) is the intended metric for the v1.0 freeze; either walk one level deeper in `countCommands()` or rename the label to `noun-groups=3`.

Once 1-3 land, re-request review. 4-6 may be done in the same wave or split to a follow-on story if the dev prefers tighter commits — but they should not ship to `main` without items 1-3 fixed.

---

## Notes for coordinator

- Worker's commit message says "Reviewer: PENDING — awaiting qa adversarial review". On bounce, the worker should NOT amend the existing commit (per project rules — never `--amend` after a hook fails or after review). New commits should land the fixes; this review file stays in place and is cited by the eventual `Reviewer: ACCEPT — qa (Quinn) — 108-A-05` token in a follow-up commit body (or by `git notes`).
- The dev worker's claim "all 2019 CLI tests + 9874 core tests pass" is technically true but misleading — the broken surfaces (static completion artifacts, probe bodies) are not under vitest. Future stories in this epic should bake artifact-parity tests so the test suite catches this class of drift autonomously.
- E94 e2e files are gated by `RUN_E2E=1` — reviewer did not run them. If the coordinator's `pnpm -r test:coverage` does include `RUN_E2E=1` somewhere, that's the canonical gate; otherwise these tests are exercising the right paths (per diff) but not actually invoking the binary in this session.
