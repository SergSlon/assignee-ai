
========== SCREEN 1 ==========

✦ Welcome to Assignee.ai v<VERSION>
  AI-Native Cloud Operator — natural language → AWS infrastructure

  [NONE] No AWS credentials detected

  Get started in 2 minutes:

  1. Set AWS credentials (easiest):
       bash/zsh:
       export AWS_ACCESS_KEY_ID=<your-key>
       export AWS_SECRET_ACCESS_KEY=<your-secret>
       PowerShell:
       $Env:AWS_ACCESS_KEY_ID='<your-key>'
       $Env:AWS_SECRET_ACCESS_KEY='<your-secret>'

  2. Or create least-privilege IAM users (recommended):
       assignee dev setup

  3. Try your first plan:
       assignee infra plan "Create an S3 bucket for my static site"

  More: https://github.com/SergSlon/assignee-ai

  Discover what assignee can build:
    assignee infra plan --help    # list supported resource types + compound patterns
OIDC: not configured. Set ASSIGNEE_OIDC_ADAPTER to enable.
┌  Assignee.ai — Project Initialization
│
▲  No AWS credentials detected. The project config will still be created.
│  Next steps: run `assignee dev setup` to create least-privilege IAM users (recommended), OR export `AWS_ACCESS_KEY_ID` + `AWS_SECRET_ACCESS_KEY` before running `assignee infra plan`.
│  Note on `AWS_PROFILE`: setting the env var alone isn't enough — it needs a matching profile in `~/.aws/credentials` with `aws_access_key_id` + `aws_secret_access_key`. The next prompt records a profile name in your project config for documentation; it doesn't authenticate API calls.
│  Assignee roles available: none (operator, reader, auditor all unset)
◆  AWS Region
│  us-east-1█
└
========== /SCREEN 1 (action: ENTER) ==========

========== SCREEN 2 ==========
◇  AWS Region
│  us-east-1
│
◆  AWS Profile
│  default█
└
========== /SCREEN 2 (action: ENTER) ==========

========== SCREEN 3 ==========
◇  AWS Profile
│  default
│
◆  Environment (tag value applied to every resource you create)
│  ● development
│  ○ staging
│  ○ production
└
========== /SCREEN 3 (action: ENTER) ==========

========== SCREEN 4 ==========
◇  Environment (tag value applied to every resource you create)
│  development
│
◆  Auto-fix security best-practice violations? (encryption, IMDSv2, public access blocking, etc.)
│  ● ask — prompt before each fix (default)
│  ○ apply — fix automatically
│  ○ skip — never auto-fix
└
========== /SCREEN 4 (action: ENTER) ==========

========== FINAL ==========
◇  Auto-fix security best-practice violations? (encryption, IMDSv2, public access blocking, etc.)
│  ask — prompt before each fix (default)
│
└  Initialized assignee.ai for region us-east-1. Run `assignee infra plan` to get started.


========== /FINAL ==========

[driver] exit code: 0
