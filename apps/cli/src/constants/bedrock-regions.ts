/**
 * KNOWN_BEDROCK_REGIONS — thin re-export shim.
 *
 * The canonical list lives in `@assignee/core/llm` (lifted in Story 50-4
 * Wave 5.1 alongside the in-core Bedrock region detector).
 */
export { KNOWN_BEDROCK_REGIONS } from "@assignee/core/llm";
