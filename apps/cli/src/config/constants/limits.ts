/**
 * Thin re-export shim — implementation moved to @assignee/core
 * (Story 50-4 Wave 5 Pass G).
 */
export {
  SCHEMA_EXCERPT_MAX_CHARS,
  CHECKPOINT_DEFAULT_TTL_HOURS,
  MEMORY_MAX_PROVISIONS,
  MEMORY_MAX_FAILURES,
  MEMORY_MAX_PATTERNS,
  HOURS_PER_MONTH,
} from "@assignee/core";
