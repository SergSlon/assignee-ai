/**
 * Thin re-export shim — canonical implementation lives in
 * `@assignee/core` (Story 50-4 Wave 5 Pass H).
 */
export {
  writeProvisionRecord,
  writeFailureRecord,
  clearFailureHistory,
  upsertPatternRecord,
} from "@assignee/core";
