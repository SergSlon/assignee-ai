/**
 * Thin re-export shim — canonical implementation lives in
 * `@assignee/core/utils/display-findings` (Story 50-4 Wave 5 Pass C-2).
 */
export {
  formatFindings,
  formatFreeTierNote,
  formatMemoryHints,
  formatAdviceHints,
} from "@assignee/core";
