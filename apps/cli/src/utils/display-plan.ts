/**
 * Thin re-export shim — canonical implementation lives in
 * `@assignee/core/utils/display-plan` (Story 50-4 Wave 5 Pass C-2).
 */
export {
  renderPlanBox,
  formatCostLine,
  formatPricingBreakdown,
  formatAppliedFixes,
  formatFixValue,
  formatAutoFixHint,
  regionLabel,
} from "@assignee/core";
