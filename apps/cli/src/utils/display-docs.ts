/**
 * Thin re-export shim — canonical implementation lives in
 * `@assignee/core/utils/display-docs` (Story 50-4 Wave 5 Pass C-2).
 */
export {
  renderDocHelp,
  renderTradeoffHelp,
  fetchDocText,
  synthesizeDocHint,
} from "@assignee/core";
