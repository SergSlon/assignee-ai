/**
 * Thin re-export shim — implementation moved to @assignee/core
 * (Story 50-4 Wave 5 Pass C).
 */
export type { IntentDefaultOverride, IntentRule } from "@assignee/core";
export {
  INTENT_RULES,
  getIntentDefaults,
  applyIntentOverrides,
} from "@assignee/core";
