/**
 * Thin re-export shim — canonical implementation lives in
 * `@assignee/core/utils/fix-selection` (Story 50-4 Wave 5 Pass C-2).
 */
export {
  promptFixSelection,
  type FixSelectionResult,
  type FixSelectionState,
} from "@assignee/core";
