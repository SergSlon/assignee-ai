/**
 * @deprecated Import `efsFileSystemStrategy` from `@assignee/core`.
 * This thin re-export is preserved during Story 50-4 so existing test
 * imports (`../efs-filesystem.js`) continue to resolve without churn.
 */
export { efsFileSystemStrategy } from "@assignee/core";
