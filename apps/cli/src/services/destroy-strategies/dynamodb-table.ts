/**
 * @deprecated Import `dynamodbTableStrategy` from `@assignee/core`.
 * This thin re-export is preserved during Story 50-4 so existing test
 * imports (`../dynamodb-table.js`) continue to resolve without churn.
 */
export { dynamodbTableStrategy } from "@assignee/core";
