/**
 * @deprecated Import `ec2EipStrategy` from `@assignee/core`.
 * This thin re-export is preserved during Story 50-4 so existing test
 * imports (`../ec2-eip.js`) continue to resolve without churn.
 */
export { ec2EipStrategy } from "@assignee/core";
