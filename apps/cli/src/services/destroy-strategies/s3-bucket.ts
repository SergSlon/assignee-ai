/**
 * @deprecated Import `s3BucketStrategy` from `@assignee/core`.
 * This thin re-export is preserved during Story 50-4 so existing test
 * imports (`../s3-bucket.js`) continue to resolve without churn.
 */
export { s3BucketStrategy } from "@assignee/core";
