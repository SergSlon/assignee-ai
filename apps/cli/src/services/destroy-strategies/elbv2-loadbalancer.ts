/**
 * @deprecated Import `elbv2LoadBalancerStrategy` from `@assignee/core`.
 * This thin re-export is preserved during Story 50-4 so existing test
 * imports (`../elbv2-loadbalancer.js`) continue to resolve without churn.
 */
export { elbv2LoadBalancerStrategy } from "@assignee/core";
