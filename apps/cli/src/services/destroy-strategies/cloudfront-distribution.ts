/**
 * @deprecated Import `cloudfrontDistributionStrategy` from `@assignee/core`.
 * This thin re-export is preserved during Story 50-4 so existing test
 * imports (`../cloudfront-distribution.js`) continue to resolve without churn.
 */
export { cloudfrontDistributionStrategy } from "@assignee/core";
