/**
 * Per-provider Vercel AI SDK LanguageModel factory — thin re-export shim.
 *
 * The canonical implementation lives in `@assignee/core/llm`
 * (lifted in Story 50-4 Wave 5 Pass B alongside the LlmAdapter class).
 */
export { createLanguageModel } from "@assignee/core/llm";
