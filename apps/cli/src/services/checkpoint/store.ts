/**
 * @deprecated Import from `@assignee/core/checkpoint`. This thin
 * re-export is preserved during Story 50-4 so existing CLI call-sites
 * and tests continue to resolve (`../checkpoint/store.js`).
 */
export { saveCheckpoint, loadCheckpoint } from "@assignee/core/checkpoint";
