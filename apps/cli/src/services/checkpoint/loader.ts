/**
 * @deprecated Import from `@assignee/core/checkpoint`. Thin re-export
 * preserved during Story 50-4. The CLI does not inject an integrity
 * verifier — `loadCheckpointFromPath` is called with no options.
 */
export { loadCheckpointFromPath } from "@assignee/core/checkpoint";
