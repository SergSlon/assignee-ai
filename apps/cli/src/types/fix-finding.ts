/**
 * Thin re-export shim — canonical implementation lives in
 * `@assignee/core/types/fix-finding` (Story 50-4 Wave 5 Pass C-2).
 */
export type { SecurityFinding, AppliedFix } from "@assignee/core";
