/**
 * @deprecated Import `ec2RouteTableStrategy` from `@assignee/core`.
 * Thin re-export preserved during Story 50-4 so existing MCP test
 * imports (`../route-table-strategy.js`) continue to resolve.
 *
 * Legacy alias `routeTableStrategy` preserved for test imports.
 */
export {
  ec2RouteTableStrategy,
  ec2RouteTableStrategy as routeTableStrategy,
} from "@assignee/core";
