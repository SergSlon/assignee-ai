/**
 * Canonical UI labels used in resource plugin field definitions.
 * Single source of truth — every `label:` that appears in 2+ plugins must reference these constants.
 */

export const FieldLabel = {
  TAGS: "Tags",
  DESCRIPTION: "Description",
} as const;
