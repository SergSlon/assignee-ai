/**
 * Shared constants for pricing decomposers and strategies.
 *
 * @see Story 23.1 — Pricing Decomposers
 */

/** Extended timeout for complex pricing lookups (EC2, RDS) that query multiple service codes. */
export const EXTENDED_TIMEOUT_MS = 8000;
