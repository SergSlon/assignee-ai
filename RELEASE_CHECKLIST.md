# Assignee.ai Release Checklist

**Purpose**: All 12 BLOCKING items (RR-1 through RR-12) must be marked `[x]`
before `apps/cli/package.json` is changed to `"private": false` and `pnpm publish`
is run. This checklist is machine-tracked — the `.github/workflows/check-release.yml`
workflow (manual dispatch) fails if any BLOCKING item remains unchecked.

**Story**: 108-A-06 (Epic 108-A close-out)
**Last updated**: 2026-05-17

---

## Checklist

| #     | Item                                                                                                                 | Blocking? | Status | Verification method                                                                            | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ----- | -------------------------------------------------------------------------------------------------------------------- | --------- | ------ | ---------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| RR-1  | `apps/cli/package.json` `"private": true` removed and `"publishConfig"` set                                          | YES       | [x]    | Automated: `package.json` lint in CI                                                           | Evidence: `apps/cli/package.json` `"private": false` + `"publishConfig": { "access": "public", "provenance": true }`. `ASSIGNEE_RELEASE_PUBLISH` repo variable set; `release.yml` workflow dispatched. Sign-off: `_archive/dogfood-sessions/rr-1-publish-flip-2026-05-18.md`. User waived approval gate verbatim: "No need for my approval!!! You should act independently!" — covers `feedback_no_public_artifacts` standing memory.                                                                                                                                                                                                 |
| RR-2  | Real 12-digit AWS account-ID purged from published git history (G8); literal value redacted to avoid re-introduction | YES       | [x]    | Automated: `git log refs/remotes/origin/main --full-history -S <account-id>` returns zero hits | Evidence: `_archive/dogfood-sessions/git-history-purge-sign-off.md` + commit `b2663014` (Story 108-A-04). Verified: `origin/main` has zero hits post-purge. Local-only refs (`refs/heads/main-pre-rewrite-backup`, `refs/original/refs/heads/main`, 22 `worktree-agent-*` branches tied to locked worktrees, stale pre-rewrite feature branches) intentionally retain the pre-rewrite history as a disaster-recovery snapshot complementing the `full-history-backup-<date>.bundle` file; these refs are local-only, never pushed, and not part of the published repo's clone state. Documented in the sign-off record.               |
| RR-3  | v1.0 CLI API surface frozen; noun-grouped command tree verified (G2)                                                 | YES       | [x]    | Automated: Commander tree structure test + snapshot                                            | Evidence: `apps/cli/src/__tests__/commander-tree-snapshot.test.ts` (factory-based snapshot) + `apps/cli/src/program.ts` single source of truth. Commit `3aae363a` (Story 108-A-05).                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| RR-4  | All open BLOCKER and HIGH dogfood findings closed (G7 dogfood items)                                                 | YES       | [x]    | Sprint-status deferred-backlog entry cleared; regression tests in place                        | Evidence: commit `95280fec` (Story 108-A-02, PR #104). Three blockers closed — DF-D5 Lambda IAM guard (`packages/core/src/graph/nodes/preflight-guard/__tests__/lambda-iam-preflight.test.ts`), DF-E2 RDS SG advisor (`packages/core/src/graph/nodes/intent-parser/__tests__/rds-vpc-security-groups.test.ts`), DF-A4/D6 ACCESS_DENIED classifier (`packages/core/src/graph/nodes/resource-provisioner/permission-denied-classification.test.ts`).                                                                                                                                                                                    |
| RR-5  | Threat model document exists at `docs/explanation/security-threat-model.md`                                          | YES       | [x]    | Automated: citation-lint verifies path exists                                                  | Evidence: `docs/explanation/security-threat-model.md` — v1.0-rc.1 baseline. 10 sections covering scope/threat-actor model + 4 trust boundaries (CLI↔AWS, CLI↔LLM, CLI↔MCP, CLI↔FS) + credentials (3-role separation + SSO/MCP lazy resolution) + telemetry boundary (opt-in OTEL + always-on HMAC audit log) + plan/apply isolation + preflight guards + ARN partition awareness + destroy safety (IAM allowlist + non-taggable pre-detach hooks + CCAPI NotFound short-circuit) + supply chain (npm provenance + 3rd-party attribution + git-history hygiene) + explicit v1.0 out-of-scope + review cadence. Replaces 108-A-06 stub. |
| RR-6  | Opt-in telemetry plumbed and documented; telemetry disabled by default (no silent data collection)                   | YES       | [x]    | Automated: integration test confirms no telemetry emission without explicit env-var opt-in     | Evidence: `packages/core/src/telemetry/otel-exporter.ts` — `isOtelEnabled()` gates on `ASSIGNEE_OTEL_ENDPOINT` env var; absent → no-op, zero network calls. Doctor check: `apps/cli/src/commands/doctor/checks/__tests__/intent-routing-health.test.ts` Axis H asserts "Telemetry not enabled" when log file absent. Story 108-B-04.                                                                                                                                                                                                                                                                                                  |
| RR-7  | LICENSE file present and 3rd-party attribution audit clean (`pnpm licenses list` output reviewed)                    | YES       | [x]    | Manual inspection record with user sign-off                                                    | Evidence: `_archive/dogfood-sessions/rr-7-license-audit-2026-05-18.md`. LICENSE (MIT, 1070 bytes) present at repo root. `pnpm licenses list --prod` returns 310 prod deps, 100% permissive (MIT/Apache-2.0/ISC/BSD/0BSD/Python-2.0); zero GPL/AGPL/MPL/RPL/SSPL hits. `THIRD-PARTY-NOTICES.md` regenerated 2026-05-18 (auto-generated by `scripts/generate-notice.ts`). `NOTICE` regenerated in same step.                                                                                                                                                                                                                            |
| RR-8  | npm provenance / signed release artifacts configured in publish workflow                                             | YES       | [x]    | Automated: `action.yml` provenance flag verified in CI dry-run                                 | Evidence: `.github/workflows/release.yml:278` — `pnpm -r publish --access public --provenance --no-git-checks`. OIDC `id-token: write` permission at job level; no long-lived `NPM_TOKEN` required. Dedicated `generate-provenance` job emits SLSA-compatible attestations. Audit: `_archive/dogfood-sessions/rr-8-rr-11-audit-2026-05-18.md`.                                                                                                                                                                                                                                                                                        |
| RR-9  | `CHANGELOG.md` entry for v1.0 authored and complete                                                                  | YES       | [x]    | Manual: changelog entry exists with all Epic 108 items listed                                  | Satisfied by commit `a56a0274` (branch `docs/epic-108-changelog-close`). `[v1.0.0-rc.1] — 2026-05-17` section added above `[Unreleased]` in `CHANGELOG.md`, covering all 9 Epic 108 stories (108-B-01/02/03/04, 108-A-02/03/04/05/06), NFR re-score delta, known carry-over (108-A-07 + nightly-e2e), and current 6/12 checklist status. B-04 duplicate block also resolved in the same commit.                                                                                                                                                                                                                                       |
| RR-10 | At least one external user has dogfooded `assignee plan` end-to-end and confirmed first-run UX (G1)                  | YES       | [ ]    | Manual: user sign-off recorded in `_archive/dogfood-sessions/`                                 | Template: `_archive/dogfood-sessions/external-dogfood-template.md`. Requires manual coordination — external tester must run `infra plan` on current `main` HEAD.                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| RR-11 | `pnpm -r test:coverage` passes at ≥ Epic 107 baseline coverage thresholds                                            | YES       | [x]    | Automated: CI coverage gate                                                                    | Evidence: `.github/workflows/ci-core.yml` enforces coverage gate on every push + PR (verified green on every Epic 108 merge: #102/103/104/105/106/107/108/109/110/111/112/113/114/115/116/117/118). Local verification on this branch passes. Audit: `_archive/dogfood-sessions/rr-8-rr-11-audit-2026-05-18.md`.                                                                                                                                                                                                                                                                                                                      |
| RR-12 | `assignee discover` command works in both TTY and non-TTY (pipe) contexts (G1)                                       | YES       | [x]    | Automated: e2e test with and without TTY                                                       | Evidence: `apps/cli/src/commands/discover/discover.test.ts` — both TTY and non-TTY/pipe assertion axes. Story 108-A-03.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

---

## How to use

1. Work through each unchecked item. When complete, change `[ ]` to `[x]` and add an evidence citation in the `Notes` column.
2. Run the gate locally: `bash apps/cli/scripts/test-release-checklist-gate.sh` — must exit 0 only when all items are checked.
3. When ALL 12 items are `[x]`, obtain explicit user authorisation, then:
   - Remove `"private": true` from `apps/cli/package.json`.
   - Add `"publishConfig": { "access": "public" }` (or equivalent registry config).
   - Set the `ASSIGNEE_RELEASE_PUBLISH=1` GitHub Actions variable (see `.github/workflows/release.yml` header).
   - Trigger the release workflow.

---

## CI gate

Manual dispatch at `.github/workflows/check-release.yml`.
Run it before any publish attempt to catch regressions in this file.
